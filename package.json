{
	"private": true,
	"workspaces": [
		"packages/shared",
		"packages/workspace",
		"packages/ui"
	],
	"scripts": {
		"compile": "tsc -b && npm run build:css -w llama-copilot-ui && npm run bundle",
		"bundle": "node scripts/bundle-extension.js packages/workspace && node scripts/bundle-extension.js packages/ui",
		"test": "npm run test --workspaces --if-present",
		"lint": "eslint",
		"package": "npm run compile && npm run package:workspace && npm run package:ui",
		"package:workspace": "cd packages/workspace && npx @vscode/vsce package --no-dependencies",
		"package:ui": "cd packages/ui && npx @vscode/vsce package --no-dependencies"
	},
	"devDependencies": {
		"@eslint/js": "^9.13.0",
		"@stylistic/eslint-plugin": "^2.9.0",
		"@types/node": "^22",
		"@vscode/vsce": "^4.0.0",
		"esbuild": "^0.28.2",
		"eslint": "^9.13.0",
		"typescript": "^5.9.2",
		"typescript-eslint": "^8.39.0",
		"vitest": "^4.0.18"
	}
}
